# Cấu hình API URL cho frontend khi gọi xuống backend
# Khi chạy local, biến này có thể để trống, nó sẽ tự gọi http://localhost:5000 (xem config.js)
# Khi deploy lên Vercel, hãy thêm biến môi trường này vào Vercel Settings và điền URL backend (VD: https://my-backend.onrender.com)
VITE_API_URL=
